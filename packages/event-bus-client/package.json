{
  "name": "@noryx/event-bus-client",
  "version": "0.1.0",
  "private": true,
  "description": "Pluggable event bus abstraction — services depend on the EventBus interface, never on a specific broker. In-memory adapter for local dev/tests, Azure Service Bus adapter for staging/production (System Architecture v1 §5, §6). Swapping brokers later (or graduating to Kafka per Open Technical Questions §11) never touches a consumer's code.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@noryx/shared-types": "workspace:*",
    "@azure/service-bus": "^7.9.0",
    "@azure/identity": "^4.4.0"
  },
  "devDependencies": {
    "@noryx/eslint-config": "workspace:*",
    "typescript": "^5.6.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "@types/node": "^20.14.0",
    "@types/jest": "^29.5.0"
  }
}
