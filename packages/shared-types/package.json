{
  "name": "@noryx/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types, DTOs, and versioned event contracts used across every Noryx service — the single source of truth that keeps Sphere/Orbis/shared-services integration contracts from drifting (the #1 lesson-learned carried from the legacy project).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest --passWithNoTests"
  },
  "devDependencies": {
    "@noryx/eslint-config": "workspace:*",
    "typescript": "^5.6.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "@types/node": "^20.14.0",
    "@types/jest": "^29.5.0"
  }
}
