# Acceptance Matrix: BUDGETING-PHASE-1-FOUNDATION

Per `CLAUDE_ENGINEERING_PROTOCOL.md` §5/§13. Every scenario below is `NOT EXECUTED` — this is a discovery-stage matrix; nothing has been implemented or tested. Statuses will be updated by the implementation session only after real execution against PostgreSQL, never inferred.

**Amendment note (v2):** per "NORYX CTO PROPOSAL AMENDMENT AUTHORIZATION" (CTO Decisions A/B/C/D — see `CONTRACT.md` §0), all pre-existing scenario IDs below are unchanged and unrenumbered. BUD-008's description was clarified in place (same ID) to state that the successful-approval scenario must use a budget with at least one line, consistent with Decision B. Five new scenarios (BUD-046..BUD-050) were appended, covering Decisions B/C/D, per the CTO's minimum-coverage list.

**Correction note (v3):** per "NORYX CTO PROPOSAL CORRECTION AUTHORIZATION" (see `CONTRACT.md` §0a), CTO review found the concurrency mechanism v2 attributed to BUD-046 (a single `UPDATE ... WHERE status='DRAFT' AND EXISTS(...)` statement) insufficient to guarantee the underlying business invariant. BUD-046's ID and position are unchanged; its description is corrected in place to test the business invariant (422, budget stays DRAFT) rather than a specific SQL statement. One new scenario, BUD-051, is appended after BUD-050, covering the corrected parent-row-lock concurrency design.

**Correction note (v4):** per the second "NORYX CTO PROPOSAL CORRECTION AUTHORIZATION" (see `CONTRACT.md` §0b), CTO review found two further gaps: `BudgetsService.update()` (header PATCH) did not participate in the v3 parent-row lock, and no mechanism re-validated existing budget lines when a header date-range PATCH changed the dates those lines were originally validated against. No existing scenario's description needed correction for this round — BUD-001..BUD-051 are all unchanged. Two new scenarios, BUD-052 and BUD-053, are appended after BUD-051, covering header-PATCH serialization and the date-change/existing-line-invalidation invariant respectively. No ID anywhere in this document has ever been renumbered or removed, across any correction round. This remains a discovery-stage matrix — every scenario, including BUD-052/BUD-053, is `NOT EXECUTED`.

**Correction note (v5):** per "NORYX CTO PROPOSAL CORRECTION AUTHORIZATION — v5 DOCUMENTATION CLEANUP" (see `CONTRACT.md` §0b/amendment history), the CTO corrected three terminology defects in `CONTRACT.md` (a miscounted "six operations" → five, a stray "Decision E" reference, and confirmation that the Gap 2 date-change 422 status code is CTO DECISION rather than proposed). No scenario ID, description, or status in this file required any change — BUD-001..BUD-053 are all unchanged, all `NOT EXECUTED`. BUD-053 continues to assert HTTP 422 for the invalidating date-change case; BUD-046's existing, distinct 422 (empty-budget approval rejection) is unaffected.

**Correction note (v6, this version):** per "NORYX CTO PROPOSAL CORRECTION AUTHORIZATION — v6" (see `CONTRACT.md` §11), the CTO required the concurrency proofs' reliance on PostgreSQL's `READ COMMITTED` isolation level to be made explicit — the codebase's own documented default, not a new choice. BUD-051's and BUD-052's descriptions below are updated only to name that isolation level and note the absence of any test-only override; both remain genuine concurrent-transaction tests asserting observable business invariants, not source-code inspection. No scenario ID, status, or any other scenario's description changed — BUD-001..BUD-053 are all unchanged in substance, all still `NOT EXECUTED`. No locking mechanism, decision, or architecture changed.

## Budget Header CRUD

| ID      | Scenario                                                                                                                                                                                                           | Status |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| BUD-001 | Create budget header — success (DRAFT, currencyCode server-resolved from legal entity)                                                                                                                             | PASS   |
| BUD-002 | Create rejected: duplicate `code` within same `(tenant_id, legal_entity_id)`                                                                                                                                       | PASS   |
| BUD-003 | Same `code` permitted in a different legal entity (same tenant)                                                                                                                                                    | PASS   |
| BUD-004 | Create rejected: `end_date` <= `start_date`                                                                                                                                                                        | PASS   |
| BUD-005 | Update header while DRAFT — success                                                                                                                                                                                | PASS   |
| BUD-006 | Update header rejected once APPROVED (immutability)                                                                                                                                                                | PASS   |
| BUD-007 | List/get header — response shape includes status/dates/currency                                                                                                                                                    | PASS   |
| BUD-008 | `POST /:id/approve` on a DRAFT budget containing at least one valid line — success, status becomes APPROVED (clarified per CTO Decision B — the approving budget must have ≥1 line; the zero-line case is BUD-046) | PASS   |
| BUD-009 | Repeat `approve()` on an already-APPROVED budget — rejected 409                                                                                                                                                    | PASS   |

## Budget Line CRUD

| ID      | Scenario                                                                                           | Status |
| ------- | -------------------------------------------------------------------------------------------------- | ------ |
| BUD-020 | Create line — success (valid accountId + periodId + non-negative amountMinor, parent budget DRAFT) | PASS   |
| BUD-021 | Create line rejected: `account_id` not found/inactive/wrong legal entity                           | PASS   |
| BUD-022 | Create line rejected: `period_id` not found/wrong legal entity                                     | PASS   |
| BUD-023 | Create line rejected: `amount_minor` negative                                                      | PASS   |
| BUD-024 | Create line rejected: parent budget is APPROVED (not DRAFT)                                        | PASS   |
| BUD-025 | Duplicate `(budget_id, account_id, period_id)` rejected (app-level, 409)                           | PASS   |
| BUD-026 | Update line while parent budget is DRAFT — success                                                 | PASS   |
| BUD-027 | Update/delete line rejected once parent budget is APPROVED                                         | PASS   |
| BUD-028 | Delete line while parent budget is DRAFT — success                                                 | PASS   |
| BUD-029 | List/filter lines by account, period                                                               | PASS   |

## Tenant / Legal-Entity Isolation (RLS)

| ID      | Scenario                                                                                                                                                                          | Status |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| RLS-001 | A second legal entity (same tenant) cannot read/reference the first legal entity's budgets or lines via the API                                                                   | PASS   |
| RLS-002 | A second tenant cannot see the first tenant's rows via the API                                                                                                                    | PASS   |
| RLS-003 | Raw-SQL proof: with `app.current_tenant_id` set to tenant B (via the non-superuser `noryx_app` role), a `SELECT` against `budgets`/`budget_lines` returns zero of tenant A's rows | PASS   |

## RBAC

| ID       | Scenario                                                                                                      | Status |
| -------- | ------------------------------------------------------------------------------------------------------------- | ------ |
| RBAC-001 | `finance.viewer` — GET allowed on both resources; POST/PATCH/approve/DELETE rejected 403                      | PASS   |
| RBAC-002 | `finance.poster` — budget header POST/PATCH/approve rejected 403; line POST/PATCH/DELETE allowed; GET allowed | PASS   |
| RBAC-003 | `finance.admin` — all operations on both resources allowed                                                    | PASS   |
| RBAC-004 | No token — 401 on every new route                                                                             | PASS   |
| RBAC-005 | `route-role-matrix.spec.ts` — 100% of routes (pre-existing + new) accounted for, no unguarded route           | PASS   |

## Database Invariants / Raw-SQL Proofs

| ID     | Scenario                                                                                                          | Status |
| ------ | ----------------------------------------------------------------------------------------------------------------- | ------ |
| DB-001 | Unique index on `budgets(tenant_id, legal_entity_id, code)` rejects a raw duplicate `INSERT` at the DB level      | PASS   |
| DB-002 | Unique index on `budget_lines(budget_id, account_id, period_id)` rejects a raw duplicate `INSERT` at the DB level | PASS   |
| DB-003 | FK constraint on `budget_lines.account_id` rejects a raw `INSERT` referencing a non-existent account              | PASS   |
| DB-004 | FK constraint on `budget_lines.period_id` rejects a raw `INSERT` referencing a non-existent accounting period     | PASS   |
| DB-005 | CHECK constraint on `budget_lines.amount_minor >= 0` rejects a raw negative `INSERT` at the DB level              | PASS   |

## Concurrency

| ID       | Scenario                                                                                                                                                                 | Status |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| CONC-001 | Two simultaneous `POST /budgets` with the same `code` in the same `(tenant_id, legal_entity_id)` — exactly one 201, one rejected, never two rows persisted               | PASS   |
| CONC-002 | Two simultaneous `POST /budgets/:budgetId/lines` with the same `(account_id, period_id)` under the same budget — exactly one 201, one rejected, never two rows persisted | PASS   |

## Migration Safety

| ID      | Scenario                                                                                                                                      | Status |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| MIG-001 | `0023_budgeting_phase_1_foundation.sql` applies cleanly to a fresh database                                                                   | PASS   |
| MIG-002 | `0023` + `017_budgeting_rls.sql` apply cleanly to a pre-populated ("seeded") database representing prior schema state, no data loss/ambiguity | PASS   |

## Regression Gate

| ID      | Scenario                                                            | Status |
| ------- | ------------------------------------------------------------------- | ------ |
| REG-001 | Full pre-existing unit test suite passes unmodified (0 regressions) | PASS   |
| REG-002 | Full pre-existing e2e test suite passes unmodified (0 regressions)  | PASS   |
| REG-003 | Typecheck clean                                                     | PASS   |
| REG-004 | Lint clean (0 new errors)                                           | PASS   |
| REG-005 | Build clean                                                         | PASS   |

## Accounting Invariant Proof

| ID      | Scenario                                                                                                                                                                                               | Status |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| BUD-045 | Confirm zero rows written to `journal_entries`/`journal_lines` as a side effect of any budget or budget-line operation, including `approve()` (explicit negative assertion — this phase posts nothing) | PASS   |

## Additional Scenarios — CTO Amendment (Decisions B / C / D)

New IDs appended after the frozen original numbering (protocol §13) — none of the IDs above were renumbered or removed. Decision A required no new scenario: the non-negative-magnitude rule is already covered by BUD-023 ("Create line rejected: `amount_minor` negative").

**BUD-046 correction note (v3, "NORYX CTO PROPOSAL CORRECTION AUTHORIZATION"):** the v2 description below cited a specific SQL statement (`UPDATE ... WHERE status='DRAFT' AND EXISTS(...)`) that CTO review found insufficient to guarantee the invariant under concurrency (see `CONTRACT.md` §0a/§11). BUD-046's ID and place in the matrix are unchanged; its description is corrected in place to test the observable business invariant, not any particular SQL implementation, so the scenario remains valid regardless of which correct mechanism is used to satisfy it.

| ID      | Scenario                                                                                                                                                                                                                                                                                                                                                                   | Status |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| BUD-046 | `POST /:id/approve` on a DRAFT budget with zero lines — rejected 422; the budget remains DRAFT afterward. The test asserts this observable outcome only (request → 422 → budget still DRAFT with zero lines) and does not require or assert any specific SQL statement or locking implementation. Distinct from the 409 "already APPROVED" case (BUD-009) (CTO Decision B) | PASS   |
| BUD-047 | Create line — accepted when the referenced accounting period's complete date range falls within the parent budget's `start_date`/`end_date` (`period.start_date >= budget.start_date AND period.end_date <= budget.end_date`) (CTO Decision C)                                                                                                                             | PASS   |
| BUD-048 | Create line — rejected (400) when the referenced accounting period's `start_date` is before the parent budget's `start_date` (CTO Decision C)                                                                                                                                                                                                                              | PASS   |
| BUD-049 | Create line — rejected (400) when the referenced accounting period's `end_date` is after the parent budget's `end_date` (CTO Decision C)                                                                                                                                                                                                                                   | PASS   |
| BUD-050 | Two (or more) budgets, both APPROVED, with overlapping date ranges and/or lines referencing the same account(s)/period(s) — both remain valid and readable via the API; no exclusivity conflict, no error, no automatic supersession (CTO Decision D — retained as originally proposed)                                                                                    | PASS   |

## Additional Scenario — CTO Concurrency Correction (v3)

New ID appended after BUD-050 (protocol §13) — no existing ID renumbered or removed.

| ID      | Scenario                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Status |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| BUD-051 | Concurrent final-line deletion vs approval — regardless of which transaction obtains the parent budget lock first, the system must never produce an APPROVED budget with zero lines; one operation must serialize and fail appropriately, and the final persisted state must satisfy the invariant. Exercised as a genuine concurrent-transaction test against real PostgreSQL 16, running under the codebase's default READ COMMITTED isolation level (not a static/unit-only assertion, and no test-only isolation-level override) covering both interleavings from `CONTRACT.md` §11: Case A (`approve()` obtains the parent lock first → concurrent final-line DELETE waits → approval commits → DELETE subsequently sees APPROVED and is rejected → line survives) and Case B (final-line DELETE obtains the parent lock first → line deleted and committed → approve() subsequently sees zero lines → approval rejected → budget remains DRAFT) | PASS   |

## Additional Scenarios — CTO Concurrency Correction (v4)

New IDs appended after BUD-051 (protocol §13) — no existing ID anywhere in this document has ever been renumbered or removed. Per "NORYX CTO PROPOSAL CORRECTION AUTHORIZATION" (v4, see `CONTRACT.md` §0b), CTO review found two remaining gaps in the v3 serialization model: `BudgetsService.update()` (header PATCH) did not participate in the parent-row lock, and no mechanism re-validated existing budget lines when a header date-range PATCH changed the dates those lines were originally validated against.

| ID      | Scenario                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Status |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| BUD-052 | Concurrent budget header mutation vs approve/line mutation — budget header PATCH, `approve()`, and budget-line create/update/delete all serialize through the same parent budget row lock. Exercised as a genuine concurrent-transaction test against real PostgreSQL 16, running under the codebase's default READ COMMITTED isolation level (no test-only isolation-level override), asserting observable business invariants (never inspecting source code for the presence of `FOR UPDATE`). Covers, at minimum: (A) header PATCH obtains the parent lock first → concurrent approval/line mutation waits → header mutation commits → the waiting operation's next statement, under READ COMMITTED, takes a fresh snapshot and re-reads current state/invariants only after acquiring the lock → no invalid or stale state is committed; and (B) approval or line mutation obtains the parent lock first → concurrent header PATCH waits → the waiting PATCH's next statement likewise re-evaluates current state/invariants only after acquiring the lock → no invalid or stale state is committed (`CONTRACT.md` §0b/§11 Cases C/D, isolation-level basis in §11 v6) | PASS   |
| BUD-053 | Budget date change cannot invalidate existing budget lines — create a valid DRAFT budget; add one or more valid budget lines whose accounting periods are inside the budget dates; attempt to PATCH the budget dates so that at least one existing line's accounting period falls outside the new range; verify the PATCH is rejected, the budget dates remain unchanged, the existing lines remain unchanged, and no partial mutation occurs (not even to unrelated fields in the same PATCH payload). Also covers the inverse boundary: a date change under which every existing line's period remains fully contained in the proposed range succeeds normally (`CONTRACT.md` §0b Gap 2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | PASS   |

---

**Total scenarios:** 50 (Header CRUD 9 + Line CRUD 10 + RLS 3 + RBAC 5 + DB 5 + Concurrency 2 + Migration 2 + Regression 5 + Accounting Invariant Proof 1 + CTO Amendment additions 5 + CTO Concurrency Correction v3 1 + CTO Concurrency Correction v4 2). **PASS:** 50. **FAIL:** 0. **NOT EXECUTED:** 0. **BLOCKED:** 0.

This matrix is frozen once CTO_APPROVED; scenario IDs are stable and must not be renumbered during implementation (protocol §13). New scenarios discovered during implementation get new IDs appended, not inserted into the existing numbering.

**Execution note (implementation session, this version):** all 50 scenarios above have been ACTUALLY EXECUTED and PASS, per the Acceptance Discipline requirement (no status inferred from code inspection alone). BUD-001..BUD-050, BUD-052, BUD-053, RLS-001/002, RBAC-001..004, DB-001..005 were executed via `services/sphere-finance/test/budgeting.e2e-spec.ts` (Jest e2e, real PostgreSQL 16, `noryx_test`). RLS-003 was executed as a raw-SQL proof against the non-superuser `noryx_app` role (also inside the same e2e spec, using `APP_ROLE_DATABASE_URL`). RBAC-005 was executed via `services/sphere-finance/src/route-role-matrix.spec.ts` (157/157 unit tests, including the 10 new Budgeting routes' reflection-based role assertions and the "never unrecognized route" check). BUD-051/BUD-052/BUD-052b (an added scenario — see below) were exercised as genuine concurrent-transaction tests using `Promise.all` pairs run across repeated real-PostgreSQL trials (20 iterations for BUD-051, 15 each for BUD-052/BUD-052b), asserting the observable business invariant on every single iteration and confirming both lock-acquisition orderings actually occurred. MIG-001/MIG-002 were executed by hand against dedicated `noryx_migtest_fresh`/`noryx_migtest_seeded` databases (the latter pre-seeded with real tenant/legal-entity/chart-of-accounts/accounting-period rows), confirmed via `\dt`/`\d` inspection and a real FK-referencing `INSERT` after migration. REG-001/002 were executed as the complete pre-existing unit (630/630) and e2e (1131/1131 total, i.e. 1087 pre-existing + 44 new Budgeting) suites. REG-003/004/005 were executed as `tsc --noEmit`, `eslint src --ext .ts`, and `nest build` respectively, all clean.

Two self-review defects were found and fixed during implementation (see the completion report for full detail): (1) `BudgetsController.approve()` was missing `@HttpCode(200)`, defaulting to Nest's 201 and breaking the repo-wide "POST that mutates an existing resource returns 200" convention every other action-style route follows — fixed by adding the decorator. (2) The originally-drafted BUD-052b e2e scenario assumed a name-only header PATCH never conflicts with `approve()`, which contradicts BUD-006's own established immutability rule (the ENTIRE header, not just start_date/end_date/code, becomes immutable once APPROVED) — the scenario was rewritten to assert the actually-correct Case C/D serialization behavior instead of the incorrect original premise. Both were caught by actually running the tests, not by inspection, consistent with the Acceptance Discipline requirement.
