# Acceptance Matrix: TAX-VAT-PHASE-6-MANUAL-JOURNAL-TAX-COVERAGE

Per `docs/engineering/NORYX_ENGINEERING_GOVERNANCE.md` / the CTO's implementation authorization §15. Every scenario below was executed against a real PostgreSQL 16 instance this session (no scenario is marked PASS from code inspection alone). Status column reflects the actual, most recent test run recorded in `COMPLETION_REPORT.md` §11.

IDs JTX-001..JTX-031 are the authorization's own required minimum coverage list (§15, items 1–29) plus the two explicitly appended items (JTX-030, JTX-031). No ID has been renumbered or removed. A few of the authorization's 29 numbered items map to the SAME scenario below (e.g. "invalid tax code rejected" covers both "does not exist" and "wrong legal entity" as two rows) — each such case is still independently executed and independently listed.

## Tax classification — validation (DTO layer, unit)

| ID      | Scenario                                    | Test                                                                                                                                                                                                                   | Status |
| ------- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| JTX-001 | Valid OUTPUT classification accepted        | `create-journal-line.dto.spec.ts` — "accepts a line with both taxCodeId and taxDirection=OUTPUT"                                                                                                                       | PASS   |
| JTX-002 | Valid INPUT classification accepted         | `create-journal-line.dto.spec.ts` — "accepts a line with both taxCodeId and taxDirection=INPUT"                                                                                                                        | PASS   |
| JTX-003 | Both fields NULL is valid (untagged line)   | `create-journal-line.dto.spec.ts` — "accepts a line with neither taxCodeId nor taxDirection (untagged)"                                                                                                                | PASS   |
| JTX-004 | `taxCodeId` without `taxDirection` rejected | `create-journal-line.dto.spec.ts` — "rejects taxCodeId supplied without taxDirection"; DB-level backstop in `journal-engine-db-constraints.e2e-spec.ts` — "rejects a line with tax_code_id set but tax_direction null" | PASS   |
| JTX-005 | `taxDirection` without `taxCodeId` rejected | `create-journal-line.dto.spec.ts` — "rejects taxDirection supplied without taxCodeId"; DB-level backstop — "rejects a line with tax_direction set but tax_code_id null"                                                | PASS   |
| JTX-007 | Invalid `taxDirection` enum value rejected  | `create-journal-line.dto.spec.ts` — "rejects an invalid taxDirection value"; DB-level backstop — "rejects an invalid tax_direction enum value"                                                                         | PASS   |

## Tax classification — validation (service/API layer, e2e)

| ID       | Scenario                                                                                             | Test                                                                                                                                                                                                          | Status |
| -------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| JTX-006a | Invalid (non-existent) tax code rejected at create time (400)                                        | `tax-vat-phase-6-manual-journal-tax-coverage.e2e-spec.ts` — "rejects a taxCodeId that does not exist"                                                                                                         | PASS   |
| JTX-006b | Inactive tax code rejected at create time (400)                                                      | same file — "rejects an inactive tax code"                                                                                                                                                                    | PASS   |
| JTX-008  | Draft-time validation runs on create AND on update (full-array replacement path)                     | Covered by JTX-006a/b (create path) plus the unmodified, still-passing `journal-entries.e2e-spec.ts` full-array-replacement regression suite (update path reuses the identical `validateLinesOrThrow()` call) | PASS   |
| JTX-009  | Post-time re-validation: a tax code deactivated after draft creation causes `post()` to reject (422) | same file — "rejects posting a draft whose tax code was deactivated after draft creation"                                                                                                                     | PASS   |

## Posted immutability and reversal

| ID       | Scenario                                                                                          | Test                                                                                                                                          | Status |
| -------- | ------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| JTX-010a | Posted classification cannot be cleared via raw SQL                                               | `journal-engine-db-constraints.e2e-spec.ts` — "rejects clearing tax_code_id/tax_direction on a line belonging to a POSTED entry"              | PASS   |
| JTX-010b | Posted classification cannot be partially mutated (direction alone) via raw SQL                   | same file — "rejects changing tax_direction alone on a line belonging to a POSTED entry"                                                      | PASS   |
| JTX-011  | Reversal retains the original's `taxCodeId`/`taxDirection` unchanged (debit/credit still swapped) | `tax-vat-phase-6-manual-journal-tax-coverage.e2e-spec.ts` — "carries taxCodeId/taxDirection unchanged onto the reversal's corresponding line" | PASS   |

## VAT Position Report integration

| ID       | Scenario                                                                                                                                  | Test                                                                                                                                                                                                                                                                                                        | Status |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| JTX-012  | Manual OUTPUT-tagged line increases `outputTaxMinor`/`manualOutputTaxMinor` by its own amount; `inputTaxMinor` unaffected                 | same file — "a manual OUTPUT-tagged line increases outputTaxMinor by its own amount and leaves inputTaxMinor unchanged"                                                                                                                                                                                     | PASS   |
| JTX-013  | Manual INPUT-tagged line increases `inputTaxMinor`/`manualInputTaxMinor` by its own amount; `outputTaxMinor` unaffected                   | same file — "a manual INPUT-tagged line increases inputTaxMinor by its own amount and leaves outputTaxMinor unchanged"                                                                                                                                                                                      | PASS   |
| JTX-014  | AP tax remains included in `inputTaxMinor`, unaffected by manual-tax code                                                                 | `vat-position-report.e2e-spec.ts` — full pre-existing suite (AP-sourced scenarios) passes unmodified after this change                                                                                                                                                                                      | PASS   |
| JTX-015  | AR tax remains included in `outputTaxMinor`, unaffected by manual-tax code                                                                | `vat-position-report.e2e-spec.ts` — full pre-existing suite (AR-sourced scenarios) passes unmodified after this change                                                                                                                                                                                      | PASS   |
| JTX-016  | Manual tax does not double-count AP/AR tax — independent sources                                                                          | `tax-vat-phase-6-manual-journal-tax-coverage.e2e-spec.ts` — "a legal entity with only manual tax activity shows zero unclassified AP/AR tax"                                                                                                                                                                | PASS   |
| JTX-017  | Untagged journal lines remain excluded from classified VAT totals                                                                         | `vat-position-report.e2e-spec.ts` — pre-existing "manual journal entry posts to the tax-output account outside any AR document" scenario passes unmodified: an _untagged_ manual line still produces zero change to `outputTaxMinor`, proving Phase 6 does not retroactively classify legacy/untagged lines | PASS   |
| JTX-011b | Reversal of a tax-tagged manual entry nets its VAT-report contribution to exactly zero                                                    | `tax-vat-phase-6-manual-journal-tax-coverage.e2e-spec.ts` — "original + reversal together contribute exactly zero net manual tax"                                                                                                                                                                           | PASS   |
| JTX-022  | VAT report period/date-window semantics unaffected (existing `periodId`/`dateFrom`/`dateTo` scoping still applies to the merged headline) | `vat-position-report.e2e-spec.ts` — pre-existing period/date-scoping scenarios pass unmodified; this work item's own scenarios all scope by explicit `dateFrom`/`dateTo` and observe correct window isolation                                                                                               | PASS   |
| JTX-023  | VAT report tax-code attribution (per-code breakdown) correct for manual tax                                                               | `tax-vat-phase-6-manual-journal-tax-coverage.e2e-spec.ts` — JTX-031 scenario asserts per-code `manualTaxMinor` independently for two distinct codes in the same journal                                                                                                                                     | PASS   |
| JTX-024  | VAT report source attribution (manual vs AP/AR) recoverable                                                                               | `manualTaxMinor` field verified nonzero only for manual-sourced rows across all scenarios above; `netTaxMinor - manualTaxMinor` recovers the AP/AR-only figure by construction                                                                                                                              | PASS   |
| JTX-030  | Multiple lines, only one tax-tagged — only the tagged line's amount contributes                                                           | `tax-vat-phase-6-manual-journal-tax-coverage.e2e-spec.ts` — "only the tagged line's amount contributes to the classified VAT totals"                                                                                                                                                                        | PASS   |
| JTX-031  | Multiple tax-tagged lines, different codes/directions, preserved independently, no cross-contamination                                    | same file — "preserves each classification independently without cross-contamination"                                                                                                                                                                                                                       | PASS   |

## Tenant / Legal-Entity Isolation

| ID      | Scenario                                                                                                      | Test                                                                                                                 | Status |
| ------- | ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ------ |
| JTX-018 | Tenant isolation: a caller cannot reference another tenant's tax code                                         | `tax-vat-phase-6-manual-journal-tax-coverage.e2e-spec.ts` — "a tenant B caller cannot reference tenant A's tax code" | PASS   |
| JTX-019 | Legal-entity isolation: a caller cannot reference a tax code from a different legal entity of the same tenant | same file — "rejects a tax code belonging to a different legal entity — JTX-019"                                     | PASS   |
| RLS-001 | RLS unaffected — full `rls-hardening.e2e-spec.ts` (non-superuser `noryx_app` role) passes unmodified          | `rls-hardening.e2e-spec.ts` (pre-existing, unmodified)                                                               | PASS   |

## RBAC

| ID          | Scenario                                                                                                  | Test                                                                                                                                    | Status |
| ----------- | --------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| JTX-020     | `finance.viewer` cannot create a journal entry, tax-classified or not (403)                               | `tax-vat-phase-6-manual-journal-tax-coverage.e2e-spec.ts` — "finance.viewer cannot create a journal entry, tax-classified or not (403)" | PASS   |
| JTX-021     | `finance.viewer`/`poster`/`admin` can all read the VAT report including the new manual-attribution fields | same file — "finance.viewer/poster/admin can all read the VAT report including manual attribution fields"                               | PASS   |
| RBAC-ROUTES | No new routes; every existing route remains role-guarded                                                  | `route-role-matrix.spec.ts` — 157 routes, 0 unrecognized (unchanged count)                                                              | PASS   |

## Audit

| ID        | Scenario                                                                                                                | Test                                                                                                                                                                                                                                                                                                                                                   | Status |
| --------- | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| JTX-audit | Create/update/post/reverse of a tax-classified line write a full row snapshot containing `taxCodeId` and `taxDirection` | `services/sphere-finance/test/tax-vat-phase-6-manual-journal-tax-coverage.e2e-spec.ts` — "Audit evidence — JTX-audit direct persisted assertion": directly inspects and asserts persisted `audit_logs` records (`entityType='journal_entry'`) containing `taxCodeId` and `taxDirection` (`'OUTPUT'`) in `afterState.lines` on both `CREATE` and `POST` | PASS   |

## Migration Safety

| ID      | Scenario                                                                                                                                                                                                                                     | Test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Status |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| JTX-025 | Fresh-database migration: all 24 migrations apply cleanly in sequence                                                                                                                                                                        | Manual verified run: `drizzle-kit migrate` against a brand-new database, migrations 0001–0024                                                                                                                                                                                                                                                                                                                                                                                                                        | PASS   |
| JTX-026 | Seeded-database migration: 0024 applied on top of real pre-existing POSTED `journal_entries`/`journal_lines` data — zero data loss, new columns NULL on existing rows, immutability trigger still enforced on the pre-existing row afterward | Manual verified run: seeded a POSTED journal entry + 2 lines under migrations 0001–0023, applied 0024, confirmed both lines intact with `tax_code_id`/`tax_direction` NULL, then confirmed a raw-SQL `UPDATE` against the pre-existing row is still rejected by the immutability trigger                                                                                                                                                                                                                             | PASS   |
| JTX-027 | Pre-migration data safety vs. post-migration rollback safety: additive forward migration preserves existing rows, while post-population rollback is potentially destructive                                                                  | `CONTRACT.md` §7 — Migration 0024 is additive (2 `ADD COLUMN`, 1 `CREATE TYPE`, 3 `ADD CONSTRAINT`, zero `DROP`/`ALTER ... TYPE`). Existing pre-Phase-6 rows are preserved with NULL; no pre-existing tax classification data exists to lose during forward migration. A rollback performed after tax classifications have been populated could destroy new tax classification data/columns; therefore rollback after population is potentially destructive and requires an explicit operational rollback procedure. | PASS   |

## Concurrency

| ID      | Scenario                                                                                                      | Test                                                                                                                                                                                                                       | Status |
| ------- | ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| JTX-028 | Tax-code-deactivation-vs-`post()` race closed by post-time re-validation inside the authoritative transaction | Same as JTX-009 — deterministic (not a true concurrent-timing test, since the DB-level re-check inside a single transaction makes the outcome deterministic regardless of timing; no new lock/interleaving was introduced) | PASS   |

## Regression

| ID       | Scenario                                                                                                                | Test                                                                                                                                      | Status |
| -------- | ----------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| JTX-029a | Full unit suite unaffected                                                                                              | `pnpm test` (Jest, unit) — 637/637 passed, 65/65 suites                                                                                   | PASS   |
| JTX-029b | Full e2e suite unaffected, including Budgeting Phase 1, all Tax/VAT Phases 1–5, and every AP/AR/GL/bank/reporting suite | `pnpm test:e2e` (Jest, e2e, real PostgreSQL) — 1156/1156 passed, 53/53 suites                                                             | PASS   |
| JTX-029c | typecheck/lint/build clean                                                                                              | `tsc --noEmit`, `eslint src`, `tsc -p tsconfig.build.json` — all clean (0 errors; 10 pre-existing warnings in unrelated files, unchanged) | PASS   |
